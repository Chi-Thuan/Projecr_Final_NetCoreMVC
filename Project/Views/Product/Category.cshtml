@using Project.Models;
@model IEnumerable<Project.Models.Product>

@{
    ViewBag.Title = "Category";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!--================Category Product Area =================-->
<section class="cat_product_area section_padding">
    <div class="container">
        <div class="row">
            <div class="col-lg-3">
                <div class="left_sidebar_area">
                    <aside class="left_widgets p_filter_widgets customSidebar">
                       
                        <div class="w-100">
                            <p class="title_box_sideBar">Thương hiệu</p>
                            <div class="widgets_inner">
                                <ul class="list">

                                    @foreach (Category category in (List<Category>)ViewData["listCategoryName"])
                                    {
                                        <li class="link_item_sideBar">

                                            <a href="/Product/Category?categoryName=@category.id">@category.name</a>
                                            <p>@Model.Count()</p>

                                        </li>
                                         }
                                </ul>

                            </div>

                        </div>
                       
                    
                     
                    </aside>



                </div>
            </div>
            <div class="col-lg-9 wrap_list_product_category">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="product_top_bar d-flex justify-content-between align-items-center">
                            <div class="single_product_menu">
                                <p>Đã tìm thấy @Model.Count() sản phẩm</p>
                            </div>
                            <!--
    <div class="single_product_menu d-flex">
        <h5>short by : </h5>
        <select>
            <option data-display="Select">name</option>
            <option value="1">price</option>
            <option value="2">product</option>
        </select>
    </div>
    <div class="single_product_menu d-flex">
        <h5>show :</h5>
        <div class="top_pageniation">

           @foreach (Category category in (List<Category>)ViewData["listCategoryName"]) {
            <ul>

                     <li>@category.name</li>
            </ul>

        }
        </div>

    </div>
    <div class="single_product_menu d-flex">
        <div class="input-group">
            <input type="text" class="form-control" placeholder="search"
                   aria-describedby="inputGroupPrepend">
            <div class="input-group-prepend">
                <span class="input-group-text" id="inputGroupPrepend">
                    <i class="ti-search"></i>
                </span>
            </div>
        </div>
    </div>
        -->
                        </div>
                    </div>
                </div>

                <!---------   LIST PRODUCT   ------------->
                <div class="row align-items-center latest_product_inner">
                    @foreach (Product item in Model)
                    {
                        <div class="col-4 card_product h-100" style="padding : 0 !important">
                            <div class="py-5 px-4">
                                <a href="/Product/Index/@item.id">
                                    <img src="https://localhost:44342/Style/default/img/images/@item.slug@ViewData["jpg"]" alt="thumbnail" class="thumbnail" />
                                </a>
                                <div class="single_product_text">
                                    <a href="/Product/Index/@item.id"> <p class="name">@item.name</p></a>
                                    @{string price = String.Format("{0:#,##0.0}", item.price);

                                        <p class="price">@price VND</p>}
                                        <a href="https://localhost:44342/Cart/cart?status=true&id=@item.id" class="add_cart">Add to cart</a>
                                    </div>
                            </div>
                        </div>
                    }

                        
                    </div>
            </div>
        </div>
    </div>
</section>
<!--================End Category Product Area =================-->
<!-- product_list part end-->
